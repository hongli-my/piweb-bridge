{
  "name": "piweb-bridge",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run pi-bridge.ts",
    "build": "bash build.sh"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1",
    "croner": "^10.0.1"
  }
}
